upstream parcelles_app {
    server app:3000;
}

server {
    listen 80 default_server;
    server_name _;

    # ACME challenge for Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Health check
    location /health {
        access_log off;
        return 200 "ok";
    }

    location / {
        proxy_pass http://parcelles_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_buffering off;

        # File upload limit
        client_max_body_size 50M;
    }
}

# === HTTPS (décommente quand tu as un domaine) ===
# server {
#     listen 443 ssl http2;
#     server_name parcelles-tondomaine.com;
#
#     ssl_certificate /etc/letsencrypt/live/parcelles-tondomaine.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/parcelles-tondomaine.com/privkey.pem;
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         proxy_pass http://parcelles_app;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_buffering off;
#         client_max_body_size 50M;
#     }
# }
